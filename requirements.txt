# --- Core API ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.10

# --- LLM / Orchestration ---
langchain==0.3.7
# bumped 0.3.15 -> 0.3.63: minimum langchain-core that langchain-mcp-adapters
# 0.1.x supports (still <0.4, compatible with the rest of the 0.3.x stack).
langchain-core==0.3.63
langchain-openai==0.2.5
langchain-community==0.3.5
langchain-chroma==0.1.4
langchain-text-splitters==0.3.2
# bumped 0.2.45 -> 0.2.76 (latest 0.2.x): provides `langgraph.types.Command`,
# which langchain-mcp-adapters imports at runtime.
langgraph==0.2.76
openai==1.54.3

# --- MCP (external tools via Model Context Protocol) ---
langchain-mcp-adapters==0.1.14
mcp==1.12.4
# sse-starlette pinned <2.2: newer 3.x requires starlette>=0.49 which breaks
# fastapi==0.115.0 (needs starlette<0.39).
sse-starlette==2.1.3

# --- Observability ---
langsmith==0.1.142

# --- Vector Store ---
chromadb==0.5.18

# --- Embeddings (optional sentence-transformers fallback) ---
sentence-transformers==3.2.1

# --- Document Loaders ---
pypdf==5.0.1
unstructured==0.15.13
markdown==3.7

# --- Evaluation ---
ragas==0.2.3
datasets==3.0.2

# --- Utils ---
tenacity==9.0.0
structlog==24.4.0
python-dotenv==1.0.1
httpx==0.27.2
tiktoken==0.8.0

# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-mock==3.14.0
respx==0.21.1
